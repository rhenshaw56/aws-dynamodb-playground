{
  "name": "movies",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf bundle.js && browserify index.js > bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "henshaw",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.149.0"
  }
}
